<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Comments</title>
  </head>
  <body>
    <h1>Comments</h1>
    <% comments.forEach((comment) => { %>
      <div>
        <a href="/comments/<%= comment.id %>"><%= comment.cbody %></a>
      </div>
    <% }); %>
  </body>
</html>
