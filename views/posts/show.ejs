<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= post.name %></title>
  </head>
  <body>
    <div >
      <h2><%= post.name %></h2>
    </div>
    <div class="responses">
      <% comments.forEach((comment) => { %>
        <div>
          <a href="/post/<%= post.id %>"><%= post.title %>, <%= post.body %></a>
        </div>
      <% }); %>
    </div>
  </body>
</html>
