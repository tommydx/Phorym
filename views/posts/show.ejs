<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= post.name %></title>
  </head>
  <body>
    <div >
      <h2><%= post.name %></h2>
    </div>
    <div class="roster">
      <% comments.forEach((comment) => { %>
        <div>
          <a href="/post/<%= post.id %>"><%= post.name %></a>
        </div>
      <% }); %>
    </div>
  </body>
</html>
